<% @product.reviews.each do |review| %>
  <div class="review">
    <div class="poster">
      Posted by <%= review.user %> on <%= review.created_at %>
    </div>
    <p class="comment" class="container">
      <%= review.comment %>
    </p>
  </div>
<% end %>
